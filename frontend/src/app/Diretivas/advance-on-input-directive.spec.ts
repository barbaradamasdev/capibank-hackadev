import { AdvanceOnInputDirectiveDirective } from './advance-on-input-directive.directive';

describe('AdvanceOnInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceOnInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
